﻿﻿﻿﻿﻿# 互联网+智慧护理移动护理平台 — 系统架构设计

> 版本：v2.0
> 日期：2026-07-07
> 状态：架构初稿

---

## 一、项目概述

为居家或社区老人提供专业护理服务的移动平台，涵盖登录注册、服务浏览、下单预约、地址管理、订单管理、评价投诉等模块。前端基于 Uni-app（Vue 生态），后端基于 Java Spring Boot 微服务架构。

### 技术栈总览

| 层 | 技术 | 版本 | 说明 |
|----|------|------|------|
| 前端框架 | Uni-app (Vue 3) | — | 一套代码编译 iOS/Android |
| 后端框架 | Spring Boot 3.x | — | |
| 微服务治理 | Spring Cloud Alibaba | — | Nacos 注册中心 + 配置中心 |
| 网关 | Spring Cloud Gateway | — | 统一入口、路由、JWT 鉴权 |
| 反向代理 | Nginx | — | HTTPS 终止，转发到 Gateway |
| 服务间调用 | OpenFeign | — | 同步 RPC |
| 消息队列 | Apache Kafka | — | 异步事件驱动 |
| 数据库 | MySQL 8.x | — | 每个微服务独立 Schema |
| 缓存 | Redis 7.x | — | Token 黑名单、数据缓存 |
| 部署 | Docker Compose | — | MVP 单机部署 |

---



### 1.2 业务目标与架构决策追溯

| 业务目标 | 对应架构决策 | 决策说明 |
|---------|------------|---------|
| 保障交易资金安全 | 幂等性四层防御（令牌+UUID+索引+约束） | 防止重复下单和支付回调重复入账 |
| 提升用户体验与操作流畅 | 微服务独立扩展 + Redis 缓存 | 高峰时各服务可独立水平扩展，缓存提升响应速度 |
| 支撑业务快速迭代 | 微服务按业务域拆分（4服务） | 各团队可独立开发部署，互不阻塞 |
| 保障服务可用性 | Sentinel 熔断降级 + 超时配置 | 防止单服务故障级联扩散 |
| 满足医疗合规要求 | 逻辑删除 + 审计日志 + 敏感数据加密 | 数据不可物理删除、操作留痕、身份证号加密存储 |
| 保障消息可靠性 | Outbox 模式 + Kafka | 业务操作与消息发送同事务，不会丢消息 |
| 提升运维效率 | ELK + SkyWalking + Prometheus + Grafana | 日志/追踪/指标三位一体可观测 |

## 二、微服务拆分

### 2.1 服务划分

| 服务名 | 职责 | 数据层 | 前端对接 |
|--------|------|--------|--------|
| user-service | 登录注册、Token 签发与校验、用户信息 CRUD、文件上传 | MySQL + Redis | 人员 B |
| catalog-service | 服务分类管理、服务项目 CRUD、搜索筛选、服务规格与价格 | MySQL | 人员 B |
| order-service | 下单预约、订单生命周期、地址管理、支付回调 | MySQL | 人员 C / D |
| feedback-service | 评价提交展示、投诉提交与跟踪 | MySQL | 人员 D |

### 2.2 服务间依赖

| 调用方 | 被调用方 | 方式 | 场景 |
|--------|---------|------|------|
| order-service | catalog-service | Feign 同步 | 下单时获取最新价格 |
| feedback-service | order-service | Kafka 异步 | 订单完成 → 标记可评价 |
| feedback-service | order-service | Feign 同步 | 展示订单关联信息 |

### 2.3 跨库查询策略与 CAP 取舍

| 场景 | 模式 | 一致性 |
|------|------|--------|
| 下单查价格 | Feign 同步调用 catalog-service | 强一致（CP） |
| 展示订单关联评价 | Feign 同步调用 order-service | 读到最新即可 |
| 订单完成标记可评价 | Kafka 异步通知 feedback-service | 最终一致（AP） |

跨库查询均通过服务 API 聚合（应用层），不使用跨库 Join 或 DB 直连。


### 2.3 完整拓扑

```
用户手机 → Nginx(:443/80)
                ↓
         Spring Cloud Gateway(:8080)
           JWT Filter → 公开路径放行
                       → 登录路径校验 Token，user_id 写入 Header
                ↓
     ┌──────┬──────┬──────┬──────┬──────┐
     ↓      ↓      ↓      ↓      ↓
  user   catalog order  feedback  Nacos
  :8081  :8082  :8083  :8084    :8848
     ↓      ↓      ↓      ↓
  MySQL   MySQL  MySQL  MySQL
  Redis                  Kafka
```

---



### 2.4 角色权限模型规划（预留扩展）

当前 MVP 阶段仅用户角色。后续扩展计划：

| 角色 | 职责 | 涉及服务 |
|------|------|---------|
| 用户 | 浏览服务、下单、评价投诉 | 全部 |
| 护士/护理员 | 接单、更新服务状态 | order-service |
| 客服 | 处理投诉、退款审核 | order-service, feedback-service |
| 管理员 | 服务项目管理、用户管理、数据统计 | catalog-service, user-service |

## 三、API 接口规范

### 3.1 通用规范

| 项 | 规则 |
|----|------|
| URL 风格 | /api/v1/{资源} |
| 请求体 | JSON |
| 响应格式 | { "code": 0, "message": "success", "data": {...} } |
| 鉴权方式 | Authorization: Bearer <token> |
| 分页请求 | ?page=1&size=20 |
| 分页响应 | { "list": [...], "total": 120, "page": 1, "size": 20 } |
| 错误码 | 1000-1999 通用 / 2000-2999 用户 / 3000-3999 订单 / 4000-4999 评价投诉 |

### 3.2 接口清单

user-service:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | /api/v1/users/sms-code | 发送短信验证码 | 免 |
| POST | /api/v1/users/register | 注册（手机号+验证码+密码） | 免 |
| POST | /api/v1/users/login | 登录 | 免 |
| POST | /api/v1/users/logout | 登出 | 需 |
| POST | /api/v1/users/password/reset | 重置密码 | 免 |
| GET | /api/v1/users/profile | 获取个人信息 | 需 |
| PUT | /api/v1/users/profile | 修改个人信息 | 需 |
| POST | /api/v1/files/upload | 文件上传 | 需 |

catalog-service:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | /api/v1/categories | 服务分类列表 | 免 |
| GET | /api/v1/items | 服务项目列表(?categoryId=) | 免 |
| GET | /api/v1/items/{id} | 服务项目详情(含规格价格) | 免 |
| GET | /api/v1/items/search | 搜索(?keyword=&categoryId=) | 免 |

order-service:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | /api/v1/orders/prepay-token | 获取下单幂等令牌 | 需 |
| POST | /api/v1/orders/create | 创建订单 | 需 |
| GET | /api/v1/orders | 订单列表(?status=&page=&size=) | 需 |
| GET | /api/v1/orders/{id} | 订单详情 | 需 |
| POST | /api/v1/orders/{id}/cancel | 取消订单 | 需 |
| POST | /api/v1/orders/pay/callback | 支付宝支付回调 | 免JWT·验签名 |
> **支付宝回调安全说明**：该接口免 JWT Token，但**必须校验支付宝公钥 RSA2 签名**。验签失败直接拒绝（HTTP 403），验签通过后才处理业务逻辑。

| GET | /api/v1/addresses | 地址列表 | 需 |
| POST | /api/v1/addresses | 新增地址 | 需 |
| PUT | /api/v1/addresses/{id} | 编辑地址 | 需 |
| DELETE | /api/v1/addresses/{id} | 删除地址 | 需 |
| PUT | /api/v1/addresses/{id}/default | 设置默认地址 | 需 |

feedback-service:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | /api/v1/reviews | 提交评价 | 需 |
| GET | /api/v1/reviews | 评价列表(?itemId=&page=&size=) | 需 |
| POST | /api/v1/complaints | 提交投诉 | 需 |
| GET | /api/v1/complaints | 投诉列表 | 需 |
| GET | /api/v1/complaints/{id}/tracks | 查看投诉处理记录 | 需 |

---

## 四、数据库设计

设计原则：参考阿里《Java 开发手册》数据库规范 + 一线大厂实践。所有业务表包含逻辑删除标记，关键表添加乐观锁，交易表保留审计日志。
### 4.1 通用设计规范

所有 DDL 遵循：
- 引擎：ENGINE=InnoDB
- 字符集：DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- 每个字段加 COMMENT
- 业务表统一加 is_deleted 逻辑删除标记
- 关键表加 version 乐观锁

### 4.2 通用表（每个服务数据库都需要）

```sql
CREATE TABLE idempotent_record (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    idempotent_key  VARCHAR(128) NOT NULL COMMENT '幂等键',
    biz_type        VARCHAR(32) NOT NULL COMMENT '业务类型',
    biz_id          BIGINT COMMENT '业务主键',
    status          TINYINT NOT NULL DEFAULT 0 COMMENT '0处理中 1已完成',
    expire_time     DATETIME NOT NULL COMMENT '过期时间',
    create_time     DATETIME NOT NULL COMMENT '创建时间',
    UNIQUE KEY uk_key (idempotent_key)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='幂等记录表';

CREATE TABLE event_message (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    topic           VARCHAR(64) NOT NULL COMMENT 'MQ Topic',
    event_key       VARCHAR(128) NOT NULL COMMENT '事件幂等键',
    payload         JSON NOT NULL COMMENT '事件体',
    status          TINYINT NOT NULL DEFAULT 0 COMMENT '0待投递 1已投递 2失败',
    retry_count     TINYINT DEFAULT 0 COMMENT '重试次数',
    create_time     DATETIME NOT NULL COMMENT '创建时间',
    UNIQUE KEY uk_event (topic, event_key)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='本地消息表(Outbox)';
```

### 4.3 user-service — user_db

```sql
CREATE TABLE user (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
    phone           VARCHAR(20) NOT NULL COMMENT '手机号',
    password        VARCHAR(128) COMMENT 'BCrypt加密密码',
    nickname        VARCHAR(64) COMMENT '昵称',
    avatar          VARCHAR(256) COMMENT '头像URL',
    gender          TINYINT DEFAULT 0 COMMENT '0保密 1男 2女',
    id_card         VARCHAR(18) COMMENT '身份证号(医疗实名制)',
    status          TINYINT DEFAULT 0 COMMENT '0正常 1禁用',
    last_login_time DATETIME COMMENT '最后登录时间',
    register_ip     VARCHAR(45) COMMENT '注册IP',
    is_deleted      TINYINT DEFAULT 0 COMMENT '0未删 1已删',
    create_time     DATETIME NOT NULL COMMENT '创建时间',
    update_time     DATETIME COMMENT '更新时间',
    UNIQUE KEY uk_phone (phone)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

CREATE TABLE user_token (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键',
    user_id         BIGINT NOT NULL COMMENT '用户ID',
    token           VARCHAR(512) NOT NULL COMMENT 'JWT Token',
    expire_time     DATETIME NOT NULL COMMENT '过期时间',
    is_deleted      TINYINT DEFAULT 0 COMMENT '0正常 1已废弃',
    create_time     DATETIME NOT NULL COMMENT '创建时间',
    INDEX idx_user_id (user_id),
    INDEX idx_expire_time (expire_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户Token表';
```

Token 也存储在 Redis 中，用于快速校验黑名单（登出/过期）。

### 4.4 catalog-service — catalog_db

```sql
CREATE TABLE service_category (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '分类ID',
    name            VARCHAR(64) NOT NULL COMMENT '分类名称',
    icon            VARCHAR(256) COMMENT '分类图标URL',
    sort_order      INT DEFAULT 0 COMMENT '排序',
    status          TINYINT DEFAULT 1 COMMENT '0隐藏 1展示',
    is_deleted      TINYINT DEFAULT 0 COMMENT '0未删 1已删',
    create_time     DATETIME NOT NULL COMMENT '创建时间',
    update_time     DATETIME COMMENT '更新时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='服务分类表';

CREATE TABLE service_item (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '服务项目ID',
    category_id     BIGINT NOT NULL COMMENT '所属分类ID',
    name            VARCHAR(128) NOT NULL COMMENT '服务名称',
    description     TEXT COMMENT '图文详情',
    cover_image     VARCHAR(256) COMMENT '封面图URL',
    status          TINYINT DEFAULT 1 COMMENT '0下架 1上架',
    sort_order      INT DEFAULT 0 COMMENT '排序',
    is_deleted      TINYINT DEFAULT 0 COMMENT '0未删 1已删',
    create_time     DATETIME NOT NULL COMMENT '创建时间',
    update_time     DATETIME COMMENT '更新时间',
    INDEX idx_category (category_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='服务项目表';

CREATE TABLE service_spec (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '规格ID',
    service_item_id BIGINT NOT NULL COMMENT '关联服务ID',
    name            VARCHAR(64) NOT NULL COMMENT '规格名称',
    price           DECIMAL(10,2) NOT NULL COMMENT '售价',
    original_price  DECIMAL(10,2) COMMENT '原价',
    duration        INT COMMENT '服务时长(分钟)',
    status          TINYINT DEFAULT 1 COMMENT '0下架 1上架',
    is_deleted      TINYINT DEFAULT 0 COMMENT '0未删 1已删',
    create_time     DATETIME NOT NULL COMMENT '创建时间',
    update_time     DATETIME COMMENT '更新时间',
    INDEX idx_service_item (service_item_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='服务规格表';
```

### 4.5 order-service — order_db

```sql
CREATE TABLE user_address (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '地址ID',
    user_id         BIGINT NOT NULL COMMENT '用户ID',
    receiver_name   VARCHAR(32) NOT NULL COMMENT '收件人',
    receiver_phone  VARCHAR(20) NOT NULL COMMENT '联系电话',
    tag             VARCHAR(16) COMMENT '标签(家/公司/学校)',
    province        VARCHAR(32) COMMENT '省',
    city            VARCHAR(32) COMMENT '市',
    district        VARCHAR(32) COMMENT '区/县',
    detail_address  VARCHAR(256) NOT NULL COMMENT '详细地址',
    latitude        DECIMAL(10,7) COMMENT '纬度',
    longitude       DECIMAL(10,7) COMMENT '经度',
    is_default      TINYINT DEFAULT 0 COMMENT '0非默认 1默认',
    is_deleted      TINYINT DEFAULT 0 COMMENT '0未删 1已删',
    create_time     DATETIME NOT NULL COMMENT '创建时间',
    update_time     DATETIME COMMENT '更新时间',
    INDEX idx_user_id (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户地址表';

CREATE TABLE order_header (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '订单ID',
    order_no        VARCHAR(32) NOT NULL COMMENT '订单号',
    user_id         BIGINT NOT NULL COMMENT '用户ID',
    source          TINYINT DEFAULT 0 COMMENT '订单来源 0App 1小程序 2后台',
    version         INT DEFAULT 0 COMMENT '乐观锁',
    -- 服务快照
    service_item_id BIGINT NOT NULL COMMENT '服务项目ID',
    service_spec_id BIGINT NOT NULL COMMENT '规格ID',
    service_item_name VARCHAR(128) COMMENT '服务名称(快照)',
    spec_name       VARCHAR(64) COMMENT '规格名称(快照)',
    spec_price      DECIMAL(10,2) COMMENT '售价(快照)',
    spec_duration   INT COMMENT '服务时长(快照)',
    -- 地址快照
    address_id      BIGINT COMMENT '地址ID',
    receiver_name   VARCHAR(32) COMMENT '收件人(快照)',
    receiver_phone  VARCHAR(20) COMMENT '联系电话(快照)',
    address_detail  VARCHAR(256) COMMENT '详细地址(快照)',
    -- 服务时间
    service_date    DATE NOT NULL COMMENT '预约日期',
    service_time_slot VARCHAR(32) NOT NULL COMMENT '预约时段',
    -- 金额与状态
    total_amount    DECIMAL(10,2) NOT NULL COMMENT '订单金额',
    status          TINYINT NOT NULL DEFAULT 0 COMMENT '0待支付 1待服务 2已完成 3已取消 4退款中 5已退款',
    remark          VARCHAR(256) COMMENT '用户备注',
    cancel_reason   VARCHAR(256) COMMENT '取消原因',
    is_deleted      TINYINT DEFAULT 0 COMMENT '0未删 1已删',
    create_time     DATETIME NOT NULL COMMENT '创建时间',
    update_time     DATETIME COMMENT '更新时间',
    UNIQUE KEY uk_order_no (order_no),
    UNIQUE KEY uk_user_service_slot (user_id, service_item_id, service_date, service_time_slot),
    INDEX idx_user_id (user_id),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';

CREATE TABLE payment_record (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '支付记录ID',
    order_id        BIGINT NOT NULL COMMENT '订单ID',
    order_no        VARCHAR(32) NOT NULL COMMENT '订单号',
    user_id         BIGINT NOT NULL COMMENT '用户ID',
    pay_amount      DECIMAL(10,2) NOT NULL COMMENT '支付金额',
    pay_type        TINYINT NOT NULL COMMENT '1支付宝 2微信',
    pay_status      TINYINT DEFAULT 0 COMMENT '0未支付 1已支付 2已退款',
    trade_no        VARCHAR(64) COMMENT '第三方交易流水号',
    notify_id       VARCHAR(128) COMMENT '第三方通知ID(幂等用)',
    pay_time        DATETIME COMMENT '支付时间',
    refund_time     DATETIME COMMENT '退款时间',
    version         INT DEFAULT 0 COMMENT '乐观锁',
    is_deleted      TINYINT DEFAULT 0 COMMENT '0未删 1已删',
    create_time     DATETIME NOT NULL COMMENT '创建时间',
    update_time     DATETIME COMMENT '更新时间',
    UNIQUE KEY uk_order_pay_type (order_no, pay_type),
    INDEX idx_order_id (order_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='支付记录表';
```
关键设计：order_header 表冗余服务名、地址、价格快照，已产生的订单不受后续数据变更影响。version 字段用于乐观锁，防止支付并发时状态覆盖。

### 4.6 feedback-service — feedback_db

```sql
CREATE TABLE review (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '评价ID',
    order_id        BIGINT NOT NULL COMMENT '订单ID(一单一评)',
    user_id         BIGINT NOT NULL COMMENT '用户ID',
    service_item_id BIGINT COMMENT '服务项目ID',
    rating          TINYINT NOT NULL COMMENT '评分1-5星',
    content         TEXT COMMENT '评价内容',
    status          TINYINT DEFAULT 1 COMMENT '1待审核 2已展示 3隐藏',
    is_deleted      TINYINT DEFAULT 0 COMMENT '0未删 1已删',
    create_time     DATETIME NOT NULL COMMENT '创建时间',
    update_time     DATETIME COMMENT '更新时间',
    UNIQUE KEY uk_order_id (order_id),
    INDEX idx_user_id (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评价表';

CREATE TABLE review_image (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键',
    review_id       BIGINT NOT NULL COMMENT '评价ID',
    image_url       VARCHAR(256) NOT NULL COMMENT '图片URL',
    sort_order      INT DEFAULT 0 COMMENT '排序',
    is_deleted      TINYINT DEFAULT 0 COMMENT '0未删 1已删',
    INDEX idx_review_id (review_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评价图片表';

CREATE TABLE complaint (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '投诉ID',
    order_id        BIGINT NOT NULL COMMENT '订单ID',
    user_id         BIGINT NOT NULL COMMENT '用户ID',
    type            TINYINT NOT NULL COMMENT '投诉类型 1服务质量 2服务态度 3乱收费 4其他',
    content         TEXT COMMENT '投诉内容',
    status          TINYINT DEFAULT 0 COMMENT '0待处理 1处理中 2已处理 3已关闭',
    idempotent_key  VARCHAR(64) COMMENT '幂等键',
    is_deleted      TINYINT DEFAULT 0 COMMENT '0未删 1已删',
    create_time     DATETIME NOT NULL COMMENT '创建时间',
    update_time     DATETIME COMMENT '更新时间',
    UNIQUE KEY uk_idempotent (idempotent_key),
    INDEX idx_user_id (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='投诉表';

CREATE TABLE complaint_track (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键',
    complaint_id    BIGINT NOT NULL COMMENT '投诉ID',
    operator        VARCHAR(32) COMMENT '处理人',
    content         VARCHAR(256) COMMENT '处理意见',
    is_deleted      TINYINT DEFAULT 0 COMMENT '0未删 1已删',
    create_time     DATETIME NOT NULL COMMENT '创建时间',
    update_time     DATETIME COMMENT '更新时间',
    INDEX idx_complaint_id (complaint_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='投诉处理记录表';
```

### 4.7 新增表

```sql
-- 订单操作日志表：记录每步状态变更，客服仲裁/审计必备
CREATE TABLE order_operation_log (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '日志ID',
    order_id        BIGINT NOT NULL COMMENT '订单ID',
    order_no        VARCHAR(32) NOT NULL COMMENT '订单号',
    user_id         BIGINT COMMENT '操作人用户ID',
    operator        VARCHAR(32) COMMENT '操作人姓名',
    action          VARCHAR(32) NOT NULL COMMENT '动作:create/pay/cancel/complete/refund',
    from_status     TINYINT COMMENT '变更前状态',
    to_status       TINYINT NOT NULL COMMENT '变更后状态',
    remark          VARCHAR(256) COMMENT '备注',
    create_time     DATETIME NOT NULL COMMENT '创建时间',
    INDEX idx_order_id (order_id),
    INDEX idx_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单操作日志表';

-- 短信发送记录表：验证码安全审计
CREATE TABLE sms_record (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键',
    phone           VARCHAR(20) NOT NULL COMMENT '手机号',
    sms_type        VARCHAR(32) NOT NULL COMMENT '短信类型:register/login/reset_password',
    code            VARCHAR(8) NOT NULL COMMENT '验证码',
    status          TINYINT DEFAULT 0 COMMENT '0发送中 1已发送 2发送失败',
    send_time       DATETIME NOT NULL COMMENT '发送时间',
    expire_time     DATETIME NOT NULL COMMENT '过期时间',
    create_time     DATETIME NOT NULL COMMENT '创建时间',
    INDEX idx_phone_time (phone, send_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='短信发送记录表';
```

### 4.8 表结构设计评审总结

| 维度 | 结论 |
|------|------|
| 逻辑删除 | 所有业务表已增加 is_deleted 字段 |
| 乐观锁 | order_header、payment_record 已增加 version |
| 审计日志 | 新增 order_operation_log（订单状态变更审计） |
| 安全记录 | 新增 sms_record（短信验证码审计） |
| 地址标签 | user_address 新增 tag（家/公司/学校） |
| 用户安全 | user 新增 last_login_time、register_ip |
| 字符集 | 全部 DDL 显式指定 utf8mb4 |
| 字段注释 | 全部字段已添加 COMMENT |
| ID 策略 | 当前使用自增 ID，后续分库分表时改雪花算法 |
| order_no | 当前为 YYYYMMDD+序列，后续加业务码+分片位 |
---

## 五、幂等性设计

### 5.1 分层防御

| 层级 | 机制 | 适用场景 |
|------|------|---------|
| L1 | 后端发放持久幂等令牌 prepay_token | 下单（资金攸关） |
| L2 | 前端 localStorage 持久化 UUID | 评价、投诉、地址编辑 |
| L3 | 业务唯一索引 uk_user_service_slot | 订单创建兜底 |
| L4 | 支付记录 uk_order_pay_type 唯一约束 | 支付回调防重 |

### 5.2 下单幂等流程

用户 → 选服务/时间/地址 → 点提交订单

前端 -> POST /api/v1/orders/prepay-token
后端 -> 生成 prepay_token, INSERT idempotent_record(status=0)
前端 <- 返回 prepay_token

前端 -> uni.setStorageSync(prepay_token)
前端 -> POST /api/v1/orders/create (Header: Idempotent-Key=token)
后端 -> 查 status=0 → 创建订单, UPDATE status=1
前端 <- 返回 order_id

重试场景：同一个 prepay_token → 查到 status=1 → 直接返回 order_id

### 5.3 支付回调幂等

支付宝重复通知 → 命中 uk_order_pay_type 唯一约束 → 插入失败 → catch → 返回 success。

---

## 六、消息队列设计

### 6.1 Kafka Topic

| Topic | Partitions | 生产者 | 消费者 |
|-------|-----------|--------|--------|
| order_events | 3 | order-service | feedback-service |

消息类型：ORDER_CREATED / ORDER_PAID / ORDER_COMPLETED / ORDER_CANCELLED

### 6.2 Outbox Pattern

order-service 完成订单
  @Transactional
    UPDATE order_header SET status=2
    INSERT INTO event_message(status=0)

  定时任务轮询 event_message WHERE status=0
    kafkaTemplate.send("order_events", payload)
    成功 → UPDATE status=1
    失败 → retry_count++, 3次失败置 status=2 人工介入

feedback-service 消费
  @KafkaListener
    查本地处理记录 → 有则跳过（消费幂等）
    无 → 记录可评价 → 确认消费

---

## 七、项目模块结构

```
nursing-platform/
├── pom.xml                        # 父 POM：统一版本管理
├── nursing-common/                # 常量、异常、通用 DTO、工具类
├── nursing-api-gateway/           # 网关：JWT Filter、路由配置
├── nursing-user-service/          # 用户服务 :8081
├── nursing-catalog-service/       # 服务目录 :8082
├── nursing-order-service/         # 订单服务 :8083
├── nursing-feedback-service/      # 评价投诉 :8084
└── docker-compose.yml             # 全量部署编排
```

每个服务内部结构：

```
src/main/java/com/nursing/{service}/
├── {Service}Application.java
├── controller/
├── service/
├── repository/          # MyBatis-Plus Mapper
├── entity/              # 数据库实体
├── dto/                 # 请求/响应 DTO
├── event/               # Kafka 事件发布
├── consumer/            # Kafka 消费者
├── idempotent/          # 幂等处理
├── config/              # 安全、Redis 等配置
└── resources/
    ├── application.yml
    ├── application-dev.yml
    └── application-prod.yml
```

---

## 八、部署架构

### MVP 阶段（Docker Compose 单机）

全部容器化，一台 4C8G 服务器（后续生产环境可升配至 8C16G）：

### 8.1 资源测算表

| 组件 | 建议内存 | 建议 CPU | 说明 |
|------|---------|---------|------|
| Nginx | 256MB | 0.5 核 | 反向代理，资源占用极低 |
| Gateway | 512MB | 1 核 | JVM 内存 |
| Nacos | 512MB | 0.5 核 | 注册中心 + 配置中心 |
| user-service | 512MB | 1 核 | JVM 内存 |
| catalog-service | 256MB | 0.5 核 | JVM 内存 |
| order-service | 512MB | 1 核 | JVM 内存，订单核心服务 |
| feedback-service | 256MB | 0.5 核 | JVM 内存 |
| MySQL (4 个库) | 1GB | 2 核 | 共享同一个 MySQL 实例 |
| Redis | 512MB | 0.5 核 | 缓存 + Token 黑名单 |
| Kafka | 2GB | 2 核 | 消息队列，最低建议 2G 内存 |
| **合计** | **~6.3GB** | **~9.5 核** | 4C8G 服务器超配，需合理调优 JVM 参数 |

> **说明**：4C8G 服务器在 MVP 阶段通过 JVM 参数调优（限制每个服务堆内存）可满足运行需求。生产阶段建议升配至 8C16G 或扩容为多机部署。


| 组件 | 端口 | 说明 |
|------|------|------|
| Nginx | 80/443 | 外部流量入口，SSL 终结 |
| Gateway | 8080 | 路由分发 + JWT 鉴权 |
| Nacos | 8848 | 注册中心 + 配置中心 |
| user-svc | 8081 | |
| catalog-svc | 8082 | |
| order-svc | 8083 | |
| feedback-svc | 8084 | |
| MySQL | 3306 | 4 个库 |
| Redis | 6379 | 缓存 + Token 黑名单 |
| Kafka | 9092 | 异步事件 |



### 8.2 环境策略

| 环境 | 用途 | 部署方式 | 配置差异 |
|------|------|---------|---------|
| DEV | 开发自测、联调 | Docker Compose 单机 | 日志 DEBUG 级别，Mock 支付 |
| PROD | 正式上线 | Docker Compose 单机或迁移至 K8s | 日志 INFO 级别，真实支付，TLS 证书 |

### 8.3 CI/CD 流水线（MVP 方案）

| 阶段 | 工具 | 说明 |
|------|------|------|
| 代码托管 | Git | GitHub / Gitee |
| 自动构建 | GitHub Actions | push 到 main 触发 Maven 构建，单元测试 |
| 镜像打包 | Docker Build | JDK 17 slim + JAR 包，多阶段构建减小镜像体积 |
| 镜像仓库 | Docker Hub / 私有 Registry | 存储构建产物 |
| 自动部署 | Actions SSH 部署 | SSH 连接服务器，docker compose pull && up -d |
| 通知 | 钉钉机器人 | 构建成功/失败自动通知全员 |

### 8.4 生产演进路线图

| 阶段 | 时间窗口 | 触发条件 | 关键动作 |
|------|---------|---------|---------|
| MVP（当前） | D-0 至上线 | — | Docker Compose 单机 4C8G，全部服务 + 中间件容器化 |
| 阶段一 | 上线后 1-3 月 | DAU > 100 或故障率上升 | 服务多实例部署，Nginx 负载均衡，MySQL 主从 |
| 阶段二 | 上线后 3-6 月 | 订单量 > 1000/日 | MySQL 分库（按服务拆分物理库），Redis 哨兵集群 |
| 阶段三 | 上线后 6-12 月 | 用户 > 10000 | K8s 容器编排，ELK + SkyWalking + Prometheus 全量上线 |



---



## 九、非功能需求（NFR）

### 9.1 量化目标

| NFR | MVP 目标 | 说明 |
|-----|---------|------|
| 可用性 Availability | 99.5% | 月故障时间 <= 3.6 小时 |
| 性能 Performance | P99 < 2s | 核心接口（下单、登录、支付回调） |
| 安全 Security | 敏感数据加密、传输 TLS 1.3 | 身份证号 AES-256 加密 |
| 可维护性 | 后端测试覆盖率 > 60% | MVP 阶段 |
| 支付回调 | 端到端 < 5s | 用户从提交到看到结果 |

### 9.2 非功能需求对架构决策的影响

- 99.5% 可用性要求 => 关键链路（Gateway + 订单服务）至少做到无状态水平扩展
- P99 < 2s 要求 => 热点数据（服务项目、分类）使用 Redis 缓存，避免每次查 DB
- 数据安全要求 => 身份证号加密存储、API 脱敏、日志脱敏、TLS 传输

---

## 十、架构指导原则

| # | 原则 | 说明 |
|---|------|------|
| 1 | 数据安全高于开发效率 | 敏感数据必须加密存储，不留明文 |
| 2 | 可审计性优先 | 所有资金操作必须记录完整变更日志（order_operation_log） |
| 3 | 幂等是底线 | 任何对外接口（尤其是资金相关）必须具备幂等处理能力 |
| 4 | 演进优先于完美 | 不在 MVP 阶段做不可逆的技术决策，保留后续演进空间 |
| 5 | 单一职责 | 每个微服务只对自己领域内的数据负责，跨领域数据通过 API 获取 |
| 6 | 简化运维 | 优先选择团队熟悉的技术栈，控制中间件种类和部署复杂度 |
| 7 | 面向失败设计 | 假设网络延迟、服务宕机、消息丢失必然发生，做好降级预案 |

---

## 十一、可观测性

### 11.1 日志体系

- 所有服务统一输出 JSON 结构化日志
- 日志字段：@timestamp、level、logger、traceId、userId、message、service、duration
- 敏感字段（身份证号等）在日志中自动替换为 ******
- 日志级别分级：ERROR / WARN / INFO / DEBUG，运行时通过 Nacos 配置中心动态调整
- MVP 阶段：服务直接输出日志到 stdout，Docker 日志驱动采集
- 后续演进：Filebeat -> Kafka -> Logstash -> Elasticsearch -> Kibana（ELK）

### 11.2 链路追踪

- 统一 traceId 从 Gateway 入口生成，通过 HTTP Header（X-Request-Id）透传至所有微服务
- MVP 阶段：日志中携带 traceId，问题排查时通过 grep traceId 串联各服务日志
- 后续演进：引入 SkyWalking（Java Agent 无侵入挂载），覆盖 Gateway -> 微服务 -> MySQL -> Kafka 全链路

### 11.3 指标与告警

- MVP 阶段：Spring Boot Actuator 暴露基础指标（/actuator/health, /actuator/metrics）
- 核心业务埋点：下单数、支付成功率、注册数、投诉率（Micrometer + 自定义 Counter）
- 后续演进：Prometheus 采集 -> Grafana 可视化 -> AlertManager 告警
- 告警分级规划（后续完善）：P0（支付成功率 < 95%）/ P1（P99 RT > 3s）/ P2（错误率上升）

### 11.4 诊断工具

- 准备 Arthas 用于线上实时诊断（查看方法入参、调用栈、JVM 状态）
- Arthas 通过 Docker Sidecar 模式部署，随时 attach 到目标服务容器

---

## 十二、风险识别与缓解

### 12.1 Top 技术风险

| 风险 | 等级 | 影响 | 缓解措施 |
|------|------|------|---------|
| 服务器资源不足 | P0 | 服务 OOM、响应缓慢 | 已升配至 4C8G，线上持续监控内存与 CPU |
| 身份证号数据泄露 | P0 | 法律风险、用户索赔 | AES-256 加密 + API 脱敏 + 日志过滤 |
| 支付宝回调失败或延迟 | P1 | 订单支付状态不一致 | 定时对账 + 幂等约束保障不重复处理 |
| SMS 短信通道故障 | P1 | 用户无法注册登录 | 预留备用短信通道，监控发送成功率 |
| 人员 A 单点瓶颈 | P1 | 后端开发阻塞 | 协作文档完备、B/C/D 了解各自对接的 API 逻辑 |
| 订单状态不一致 | P2 | 用户端和后台状态不一致 | 完整的 order_operation_log + 最终一致性补尝机制 |
| 上线后峰值流量 | P2 | 服务变慢 | Nginx 限流 + Gateway 限流 + Sentinel 熔断保持核心链路可用 |

### 12.2 风险治理

- 每双周在全员站会中回顾 Top 风险状态
- 每个线上问题记录到风险库，补充缓解措施

## 十三、架构决策记录（ADR）

### 13.1 技术选型——后端框架对比

| 候选方案 | 优点 | 缺点 | 结论 |
|---------|------|------|------|
| Spring Cloud Alibaba（选用） | 团队 Java 技术栈，Nacos/ Sentinel/Feign 全家桶，微服务治理能力完整 | 部署复杂度较高，组件多 | 选用——团队经验匹配 |
| Spring Boot 单体 + 模块化 | 架构简单，部署运维成本低，MVP 开发速度快 | 无法独立扩展，代码耦合度随功能增加而上升 | 否——不利于后续演进 |
| Service Mesh（Istio + Dapr） | 下一代微服务治理，语言无关 | 团队无经验，学习成本高，K8s 依赖强 | 否——技术栈过于前沿 |

> **结论**：MVP 阶段选用 Spring Cloud Alibaba，利用团队 Java 背景快速上手。后续有需要可逐步引入 Service Mesh。


| 决策 | 结论 | 理由 |
|------|------|------|
| 微服务数量 | 4 个 | user / catalog / order / feedback，边界清晰 |
| 幂等键来源 | 后端发放（下单）、前端 UUID（其余） | 资金攸关操作由后端控制 |
| 消息可靠性 | 本地消息表 + Kafka | Outbox 保证业务与消息同事务 |
| 订单冗余设计 | 快照服务名/地址/价格 | 已产生订单不受后续数据变更影响 |
| 网关鉴权 | Gateway 统一做 JWT 校验 | 4 个微服务不用重复写鉴权代码 |
| API 风格 | /api/v1/{资源} + {code, message, data} | 业界主流做法 |
| 分页 | page + size | 前端分页组件直接适用 |
| Nginx | 最外层，SSL + 转发到 Gateway | 不省略 |
| 项目结构 | 单仓库 Maven 多模块 | 后端 1 人管理成本最低 |
| 部署 | Docker Compose，中间件也容器化 | 一键部署，方便迁移 |
| 文件上传 | 先放在 user-service | MVP 阶段不单独拆服务 |
| 数据库规范 | 全部 DDL 显式指定 utf8mb4 + 字段 COMMENT | 字符集不继承默认值，防乱码 |
| 逻辑删除 | 全部业务表加 is_deleted | 医疗合规需要保留数据 |
| 乐观锁 | order_header / payment_record 加 version | 防止支付并发状态覆盖 |
| 审计日志 | 新增 order_operation_log 表 | 订单每步变更留痕，客服仲裁用 |

---



## 附录 B：核心业务泳道图

`mermaid
sequenceDiagram
    participant App as App (B/C/D)
    participant Gateway as Gateway
    participant Auth as user-service
    participant Catalog as catalog-service
    participant Order as order-service
    participant Pay as 支付宝
    participant Feedback as feedback-service

    App->>Gateway: 登录/注册
    Gateway->>Auth: JWT 签发
    Auth-->>App: Token

    App->>Gateway: 浏览服务项目
    Gateway->>Catalog: 查询分类/项目
    Catalog-->>App: 项目列表 + 价格

    App->>Gateway: 下单（选规格/地址/时间）
    Gateway->>Order: 获取幂等令牌
    Order-->>App: prepay_token
    App->>Gateway: 提交订单(Idempotent-Key)
    Gateway->>Order: 创建订单
    Order->>Catalog: 同步查询最新价格
    Catalog-->>Order: 价格
    Order->>Order: 校验幂等，生成订单
    Order-->>App: order_id

    App->>Gateway: 发起支付
    Gateway->>Order: 调起支付宝
    Order->>Pay: 支付请求
    Pay-->>App: 支付页面
    App->>Pay: 用户支付
    Pay-->>Order: 异步回调
    Order->>Order: 验签 + 幂等校验，更新状态
    Order->>Order: 发送 ORDER_PAID 事件
    Order->>Order: 记录 order_operation_log

    App->>Gateway: 查看订单
    Gateway->>Order: 查询订单详情
    Order-->>App: 订单信息

    App->>Gateway: 提交评价
    Gateway->>Feedback: 提交评价（幂等）
    Feedback->>Order: 同步查询订单信息
    Feedback-->>App: 评价成功


## 附录：订单状态图

```

`mermaid
stateDiagram-v2
    [*] --> 待支付: 创建订单
    待支付 --> 待服务: 支付成功
    待支付 --> 已取消: 用户取消
    待支付 --> 已取消: 支付超时自动取消
    待服务 --> 已完成: 服务完成
    待服务 --> 已取消: 服务前取消（退款）
    已完成 --> 退款中: 用户申请退款
    退款中 --> 已退款: 退款审核通过
    退款中 --> 已完成: 退款审核拒绝
    已取消 --> [*]
    已完成 --> [*]
    已退款 --> [*]
`

> **状态条件说明**：
> - 待支付 -> 已取消（超时）：支付超时阈值 30 分钟，由定时任务扫描
> - 待服务 -> 已取消（服务前取消）：全额退款
> - 退款中 -> 已完成（拒绝）：客服审核后拒绝退款申请

```

---

> | 版本 | 日期 | 变更内容 |
> |------|------|---------|
> | v1.0 | 2026-07-07 | 初稿 |
> | v1.1 | 2026-07-07 | 补全 API 接口清单、Nginx 拓扑、Gateway 鉴权策略 |
> | v1.2 | 2026-07-07 | 数据库全面审查修复：is_deleted、version、utf8mb4、COMMENT、order_operation_log、sms_record、last_login_time、tag 等 |