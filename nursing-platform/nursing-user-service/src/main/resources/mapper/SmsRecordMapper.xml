<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nursing.user.mapper.SmsRecordMapper">

    <resultMap id="SmsRecordResultMap" type="com.nursing.user.entity.SmsRecord">
        <id property="id" column="id"/>
        <result property="phone" column="phone"/>
        <result property="smsType" column="sms_type"/>
        <result property="code" column="code"/>
        <result property="status" column="status"/>
        <result property="requestIp" column="request_ip"/>
        <result property="provider" column="provider"/>
        <result property="providerRequestId" column="provider_request_id"/>
        <result property="failureReason" column="failure_reason"/>
        <result property="sendTime" column="send_time"/>
        <result property="expireTime" column="expire_time"/>
        <result property="verifyTime" column="verify_time"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <insert id="insert" parameterType="com.nursing.user.entity.SmsRecord">
        INSERT INTO sms_record (
            id, phone, sms_type, code, status, request_ip, provider,
            send_time, expire_time, create_time, update_time
        ) VALUES (
            #{id}, #{phone}, #{smsType}, #{code}, #{status}, #{requestIp}, #{provider},
            #{sendTime}, #{expireTime}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="markSent">
        UPDATE sms_record
        SET status = 1,
            provider_request_id = #{providerRequestId},
            failure_reason = NULL,
            update_time = #{updateTime}
        WHERE id = #{id} AND status = 0
    </update>

    <update id="markFailed">
        UPDATE sms_record
        SET status = 2,
            failure_reason = #{failureReason},
            update_time = #{updateTime}
        WHERE id = #{id} AND status IN (0, 1)
    </update>

    <update id="markUnknown">
        UPDATE sms_record
        SET status = 5,
            failure_reason = #{failureReason},
            update_time = #{updateTime}
        WHERE id = #{id} AND status = 0
    </update>

    <update id="markLatestVerified">
        UPDATE sms_record
        SET status = 3,
            verify_time = #{verifyTime},
            update_time = #{verifyTime}
        WHERE phone = #{phone}
          AND sms_type = #{smsType}
          AND status = 1
        ORDER BY send_time DESC
        LIMIT 1
    </update>
</mapper>
