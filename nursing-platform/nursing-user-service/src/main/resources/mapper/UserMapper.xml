<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nursing.user.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.nursing.user.entity.User">
        <id property="id" column="id"/>
        <result property="phone" column="phone"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="gender" column="gender"/>
        <result property="idCard" column="id_card"/>
        <result property="status" column="status"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="registerIp" column="register_ip"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, phone, password, nickname, avatar, gender, id_card, status,
        last_login_time, register_ip, is_deleted, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.nursing.user.entity.User">
        INSERT INTO `user` (
            id, phone, password, nickname, avatar, gender, id_card, status,
            last_login_time, register_ip, is_deleted, create_time, update_time
        ) VALUES (
            #{id}, #{phone}, #{password}, #{nickname}, #{avatar}, #{gender}, #{idCard}, #{status},
            #{lastLoginTime}, #{registerIp}, #{isDeleted}, #{createTime}, #{updateTime}
        )
    </insert>

    <select id="selectById" resultMap="UserResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM `user`
        WHERE id = #{id}
          AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="selectByPhone" resultMap="UserResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM `user`
        WHERE phone = #{phone}
          AND is_deleted = 0
        LIMIT 1
    </select>

    <update id="updateById" parameterType="com.nursing.user.entity.User">
        UPDATE `user`
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="idCard != null">id_card = #{idCard},</if>
            <if test="status != null">status = #{status},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
            <if test="registerIp != null">register_ip = #{registerIp},</if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
          AND is_deleted = 0
    </update>

    <update id="updatePassword">
        UPDATE `user`
        SET password = #{password},
            update_time = #{updateTime}
        WHERE phone = #{phone}
          AND is_deleted = 0
    </update>

    <update id="updateLastLoginTime">
        UPDATE `user`
        SET last_login_time = #{lastLoginTime},
            update_time = #{lastLoginTime}
        WHERE id = #{id}
          AND is_deleted = 0
    </update>
</mapper>
