<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nursing.user.mapper.UserRoleMapper">
    <insert id="insertIgnore">
        INSERT IGNORE INTO user_role (user_id, role_code) VALUES (#{userId}, #{roleCode})
    </insert>

    <select id="selectRoleCodes" resultType="string">
        SELECT role_code FROM user_role WHERE user_id = #{userId} ORDER BY role_code
    </select>
</mapper>
