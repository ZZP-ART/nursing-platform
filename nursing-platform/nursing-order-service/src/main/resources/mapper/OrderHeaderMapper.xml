<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nursing.order.repository.OrderHeaderMapper">
    <resultMap id="OrderHeaderMap" type="com.nursing.order.entity.OrderHeader">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="user_id" property="userId"/>
        <result column="merchant_id" property="merchantId"/>
        <result column="source" property="source"/>
        <result column="version" property="version"/>
        <result column="service_item_id" property="serviceItemId"/>
        <result column="service_spec_id" property="serviceSpecId"/>
        <result column="service_item_name" property="serviceItemName"/>
        <result column="category_name" property="categoryName"/>
        <result column="spec_name" property="specName"/>
        <result column="spec_price" property="specPrice"/>
        <result column="spec_duration" property="specDuration"/>
        <result column="quantity" property="quantity"/>
        <result column="catalog_snapshot_version" property="catalogSnapshotVersion"/>
        <result column="address_id" property="addressId"/>
        <result column="receiver_name" property="receiverName"/>
        <result column="receiver_phone" property="receiverPhone"/>
        <result column="address_detail" property="addressDetail"/>
        <result column="service_date" property="serviceDate"/>
        <result column="service_time_slot" property="serviceTimeSlot"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="status" property="status"/>
        <result column="slot_occupied" property="slotOccupied"/>
        <result column="remark" property="remark"/>
        <result column="cancel_reason" property="cancelReason"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="BaseColumns">
        id, order_no, user_id, merchant_id, source, version, service_item_id, service_spec_id,
        service_item_name, category_name, spec_name, spec_price, spec_duration, quantity, catalog_snapshot_version, address_id,
        receiver_name, receiver_phone, address_detail, service_date, service_time_slot,
        total_amount, status, slot_occupied, remark, cancel_reason, is_deleted, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.nursing.order.entity.OrderHeader">
        INSERT INTO order_header (
            id, order_no, user_id, merchant_id, source, version, service_item_id, service_spec_id,
            service_item_name, category_name, spec_name, spec_price, spec_duration, quantity, catalog_snapshot_version, address_id,
            receiver_name, receiver_phone, address_detail, service_date, service_time_slot,
            total_amount, status, slot_occupied, remark, cancel_reason, is_deleted
        ) VALUES (
            #{id}, #{orderNo}, #{userId}, #{merchantId}, #{source}, #{version}, #{serviceItemId}, #{serviceSpecId},
            #{serviceItemName}, #{categoryName}, #{specName}, #{specPrice}, #{specDuration}, #{quantity}, #{catalogSnapshotVersion}, #{addressId},
            #{receiverName}, #{receiverPhone}, #{addressDetail}, #{serviceDate}, #{serviceTimeSlot},
            #{totalAmount}, #{status}, COALESCE(#{slotOccupied}, 1), #{remark}, #{cancelReason}, COALESCE(#{isDeleted}, 0)
        )
    </insert>

    <select id="selectById" resultMap="OrderHeaderMap">
        SELECT <include refid="BaseColumns"/>
        FROM order_header
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectByIdAndUserId" resultMap="OrderHeaderMap">
        SELECT <include refid="BaseColumns"/>
        FROM order_header
        WHERE id = #{id} AND user_id = #{userId} AND is_deleted = 0
    </select>

    <select id="selectByOrderNo" resultMap="OrderHeaderMap">
        SELECT <include refid="BaseColumns"/>
        FROM order_header
        WHERE order_no = #{orderNo} AND is_deleted = 0
    </select>

    <select id="selectByIds" resultMap="OrderHeaderMap">
        SELECT <include refid="BaseColumns"/> FROM order_header
        WHERE is_deleted = 0 AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>

    <select id="selectByMerchantId" resultMap="OrderHeaderMap">
        SELECT <include refid="BaseColumns"/> FROM order_header
        WHERE merchant_id = #{merchantId} AND is_deleted = 0
        <if test="status != null">AND status = #{status}</if>
        ORDER BY create_time DESC
        LIMIT 200
    </select>

    <select id="selectByOrderNoForUpdate" resultMap="OrderHeaderMap">
        SELECT <include refid="BaseColumns"/>
        FROM order_header
        WHERE order_no = #{orderNo} AND is_deleted = 0
        FOR UPDATE
    </select>

    <select id="selectPage" resultMap="OrderHeaderMap">
        SELECT <include refid="BaseColumns"/>
        FROM order_header
        WHERE user_id = #{userId} AND is_deleted = 0
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(1)
        FROM order_header
        WHERE user_id = #{userId} AND is_deleted = 0
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

    <select id="countUserServiceSlot" resultType="int">
        SELECT COUNT(1)
        FROM order_header
        WHERE user_id = #{userId}
          AND service_item_id = #{serviceItemId}
          AND service_date = #{serviceDate}
          AND service_time_slot = #{serviceTimeSlot}
          AND status IN (0, 1)
          AND is_deleted = 0
    </select>

    <update id="updateStatusByIdVersion">
        UPDATE order_header
        SET status = #{toStatus},
            version = version + 1,
            slot_occupied = CASE WHEN #{toStatus} IN (3, 5) THEN NULL ELSE slot_occupied END,
            cancel_reason = #{cancelReason},
            update_time = NOW()
        WHERE id = #{id}
          AND status = #{fromStatus}
          AND version = #{version}
          AND is_deleted = 0
    </update>

    <update id="updateStatusByOrderNo">
        UPDATE order_header
        SET status = #{toStatus},
            version = version + 1,
            update_time = NOW()
        WHERE order_no = #{orderNo}
          AND status = #{fromStatus}
          AND is_deleted = 0
    </update>

    <select id="selectExpiredPendingPayment" resultMap="OrderHeaderMap">
        SELECT <include refid="BaseColumns"/>
        FROM order_header
        WHERE status = 0
          AND is_deleted = 0
          AND create_time &lt;= #{deadline}
        ORDER BY create_time ASC
        LIMIT #{limit}
    </select>
</mapper>
