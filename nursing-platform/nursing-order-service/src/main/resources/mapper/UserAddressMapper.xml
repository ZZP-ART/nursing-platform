<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nursing.order.repository.UserAddressMapper">
    <resultMap id="UserAddressMap" type="com.nursing.order.entity.UserAddress">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="receiver_name" property="receiverName"/>
        <result column="receiver_phone" property="receiverPhone"/>
        <result column="tag" property="tag"/>
        <result column="province" property="province"/>
        <result column="city" property="city"/>
        <result column="district" property="district"/>
        <result column="detail_address" property="detailAddress"/>
        <result column="latitude" property="latitude"/>
        <result column="longitude" property="longitude"/>
        <result column="is_default" property="isDefault"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="BaseColumns">
        id, user_id, receiver_name, receiver_phone, tag, province, city, district,
        detail_address, latitude, longitude, is_default, is_deleted, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.nursing.order.entity.UserAddress">
        INSERT INTO user_address (
            id, user_id, receiver_name, receiver_phone, tag, province, city, district,
            detail_address, latitude, longitude, is_default, is_deleted
        ) VALUES (
            #{id}, #{userId}, #{receiverName}, #{receiverPhone}, #{tag}, #{province}, #{city}, #{district},
            #{detailAddress}, #{latitude}, #{longitude}, COALESCE(#{isDefault}, 0), COALESCE(#{isDeleted}, 0)
        )
    </insert>

    <select id="selectByUserId" resultMap="UserAddressMap">
        SELECT <include refid="BaseColumns"/>
        FROM user_address
        WHERE user_id = #{userId} AND is_deleted = 0
        ORDER BY is_default DESC, create_time DESC
    </select>

    <select id="selectByIdAndUserId" resultMap="UserAddressMap">
        SELECT <include refid="BaseColumns"/>
        FROM user_address
        WHERE id = #{id} AND user_id = #{userId} AND is_deleted = 0
    </select>

    <update id="clearDefault">
        UPDATE user_address
        SET is_default = 0, update_time = NOW()
        WHERE user_id = #{userId} AND is_deleted = 0
    </update>

    <update id="updateByIdAndUserId" parameterType="com.nursing.order.entity.UserAddress">
        UPDATE user_address
        <set>
            <if test="receiverName != null">receiver_name = #{receiverName},</if>
            <if test="receiverPhone != null">receiver_phone = #{receiverPhone},</if>
            <if test="tag != null">tag = #{tag},</if>
            <if test="province != null">province = #{province},</if>
            <if test="city != null">city = #{city},</if>
            <if test="district != null">district = #{district},</if>
            <if test="detailAddress != null">detail_address = #{detailAddress},</if>
            <if test="latitude != null">latitude = #{latitude},</if>
            <if test="longitude != null">longitude = #{longitude},</if>
            <if test="isDefault != null">is_default = #{isDefault},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND user_id = #{userId} AND is_deleted = 0
    </update>

    <update id="logicalDelete">
        UPDATE user_address
        SET is_deleted = 1, is_default = 0, update_time = NOW()
        WHERE id = #{id} AND user_id = #{userId} AND is_deleted = 0
    </update>

    <update id="setDefault">
        UPDATE user_address
        SET is_default = 1, update_time = NOW()
        WHERE id = #{id} AND user_id = #{userId} AND is_deleted = 0
    </update>
</mapper>
