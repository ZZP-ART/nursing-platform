<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nursing.catalog.repository.ServiceCategoryMapper">

    <resultMap id="ServiceCategoryMap" type="com.nursing.catalog.entity.ServiceCategory">
        <id property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="name" column="name"/>
        <result property="icon" column="icon"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectListVisible" resultMap="ServiceCategoryMap">
        SELECT id, parent_id, name, icon, sort_order, status, is_deleted, create_time, update_time
        FROM service_category
        WHERE status = 1
          AND is_deleted = 0
        ORDER BY parent_id ASC, sort_order ASC, id ASC
    </select>

    <select id="selectByParentId" resultMap="ServiceCategoryMap">
        SELECT id, parent_id, name, icon, sort_order, status, is_deleted, create_time, update_time
        FROM service_category
        WHERE parent_id = #{parentId}
          AND status = 1
          AND is_deleted = 0
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="selectVisibleById" resultMap="ServiceCategoryMap">
        SELECT id, parent_id, name, icon, sort_order, status, is_deleted, create_time, update_time
        FROM service_category
        WHERE id = #{id}
          AND status = 1
          AND is_deleted = 0
        LIMIT 1
    </select>
</mapper>
