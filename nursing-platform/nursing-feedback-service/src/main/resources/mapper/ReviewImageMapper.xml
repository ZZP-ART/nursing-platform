<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nursing.feedback.repository.ReviewImageMapper">
    <insert id="insertBatch">
        INSERT INTO review_image
        (id, review_id, image_url, sort_order, is_deleted)
        VALUES
        <foreach collection="images" item="image" separator=",">
            (#{image.id}, #{image.reviewId}, #{image.imageUrl}, #{image.sortOrder}, #{image.isDeleted})
        </foreach>
    </insert>

    <select id="selectByReviewId" resultType="com.nursing.feedback.entity.ReviewImage">
        SELECT id, review_id, image_url, sort_order, is_deleted
        FROM review_image
        WHERE review_id = #{reviewId}
          AND is_deleted = 0
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="selectByReviewIds" resultType="com.nursing.feedback.entity.ReviewImage">
        SELECT id, review_id, image_url, sort_order, is_deleted
        FROM review_image
        WHERE is_deleted = 0
          AND review_id IN
        <foreach collection="reviewIds" item="reviewId" open="(" separator="," close=")">
            #{reviewId}
        </foreach>
        ORDER BY sort_order ASC, id ASC
    </select>
</mapper>
