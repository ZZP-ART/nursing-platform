# 智慧护理平台验收修复清单

## 2026-07-09 修复验证记录

- 状态：P0/P1/P2/P3 主要验收问题已完成代码修复与自动化测试补强；完整 Docker 启动受本机 Docker daemon 未运行阻塞。
- P0-01：已修复。Gateway 增加 JWT Filter，白名单、Bearer 校验、Redis 黑名单、伪造用户头清理、可信 `X-User-Id`/`X-Gateway-Token` 注入；order/feedback/address 入口校验网关信任头。验证：`JwtAuthGlobalFilterTest`。
- P0-02：已修复。catalog 响应改为 `id/specs[].id`，common DTO 兼容 `itemId/specId`，移除重复 Feign 方法。验证：`CatalogOrderContractTest`、`OrderServiceImplTest`。
- P0-03：已修复。新增 `/internal/v1/orders/{id}` 和 `X-Internal-Token` 服务间鉴权；feedback 评价/投诉使用内部订单 DTO 校验归属与状态。验证：`OrderFeignClientContractTest`、`ReviewServiceImplTest`、`ComplaintServiceImplTest`。
- P0-04：已修复。支付回调按 RSA2 构造待签名串并验签，校验 `app_id`、`seller_id`、订单待支付、金额一致；重复合法回调幂等。验证：`PaymentServiceTest`。
- P1-01：已修复。Gateway/user-service 共享 `NURSING_JWT_SECRET`，dev 提供足长 secret，缺失/过短启动 fail-fast，`.env.example` 已提供样例。
- P1-02：已修复。新增订单完成接口、支付超时取消任务、待服务取消退款中状态与 `payment_record.refund_time` 更新，状态变化写 `order_operation_log`。
- P1-03：已修复配置。新增统一 `Dockerfile`、五个业务服务 Compose 编排、健康检查、环境变量注入；`docker compose config` 通过。完整 `up -d --build` 因本机 Docker daemon 未运行阻塞。
- P1-04：已修复。新增 AES-GCM 身份证加密组件，DDL 调整为密文字段长度，API 返回脱敏，生产 SQL stdout 关闭。验证：`IdCardCryptoTest`。
- P2-01：已修复。`BusinessException` 支持 HTTP status 映射，参数校验和未知异常保持统一响应。
- P2-02：已补强。新增 Gateway、契约、支付、feedback、身份证、下单、catalog N+1 等自动化测试。
- P2-03：已修复。Kafka/Outbox 消息值统一为 String JSON，producer 使用 `StringSerializer`，失败 3 次标记失败。
- P2-04：已修复。DB/Redis/Nacos/Kafka/JWT/支付配置支持环境变量，新增 dev/prod profile 与 `.env.example`。
- P3-01：已完成。catalog 列表使用 `selectByItemIds` 批量规格查询。
- P3-02：已完成。种子数据移除 `via.placeholder.com` 依赖，图标允许为空。
- P3-03：已完成基础收口。修正 API/架构文档中的订单创建路径和地址编辑方法差异。

验证命令：

```powershell
cd C:\Users\zhuzh\Desktop\project\nursing-platform
mvn clean test
docker compose -f docker-compose\docker-compose.yml config
mvn dependency:tree -DskipTests
```

验证结果：以上三条命令均通过。`docker compose -f docker-compose\docker-compose.yml up -d --build` 因本机 Docker Desktop Linux Engine 未运行失败，需启动 Docker 后重试。

> 生成依据：按文档验收报告  
> 生成时间：2026-07-08  
> 当前结论：P0/P1/P2/P3 已完成代码和测试闭环；仅 Docker 完整启动受本机 daemon 未运行外部阻塞  
> 使用方式：按优先级从 P0 到 P3 逐项修复；每项完成后补充负责人、修复 PR/commit、验证结果。

## 修复总览

| 优先级 | 数量 | 处理要求 |
|---|---:|---|
| P0 | 4 | 阻塞核心闭环或上线安全，必须优先修复 |
| P1 | 4 | 重要功能缺失或高风险，进入验收前必须修复 |
| P2 | 4 | 契约一致性、测试和部署质量问题，建议验收前完成 |
| P3 | 3 | 优化项，不阻塞功能验收但影响可维护性 |

## P0 阻塞项

### P0-01 Gateway JWT 鉴权与可信用户头缺失

- [x] 状态：已修复
- 影响模块：`nursing-gateway`、`nursing-order-service`、`nursing-feedback-service`
- 对应需求：系统架构设计 Gateway JWT Filter；API 文档鉴权要求
- 问题证据：
  - `nursing-gateway/src/main/resources/application.yml` 仅配置路由
  - `nursing-gateway/src/main/java/com/nursing/gateway/GatewayApplication.java` 无鉴权 Filter
  - `nursing-order-service/.../OrderController.java` 直接读取 `X-User-Id`
  - `nursing-feedback-service/.../ReviewController.java`、`ComplaintController.java` 直接读取 `X-User-Id`
- 修复任务：
  - [x] 在 Gateway 实现 `GlobalFilter` 或 `GatewayFilter`。
  - [x] 配置公开路径白名单：短信、注册、登录、重置密码、catalog 浏览、支付宝回调。
  - [x] 校验 `Authorization: Bearer <token>`。
  - [x] 校验 JWT 过期、签名、Redis 黑名单。
  - [x] 移除客户端传入的 `X-User-Id`、`X-UserId`、`userId` 等伪造头。
  - [x] 由 Gateway 校验成功后重新注入可信 `X-User-Id`。
  - [x] order/feedback 服务只接受网关注入的用户头；必要时增加内部服务调用专用鉴权。
- 验收标准：
  - [x] 未登录访问订单/地址/评价/投诉返回 401。
  - [x] 伪造 `X-User-Id` 但不带 JWT 无法访问。
  - [x] 带有效 JWT 可正常访问当前用户数据。
  - [x] 登出后 Token 被 Redis 黑名单拦截。
  - [x] Gateway 单元测试和集成测试覆盖公开路径、鉴权路径、伪造头场景。

### P0-02 catalog 与 order-service 的 Feign DTO 字段不一致

- [x] 状态：已修复
- 影响模块：`nursing-catalog-service`、`nursing-common`、`nursing-order-service`
- 对应需求：下单时 order-service 通过 Feign 获取 catalog 最新价格和规格
- 问题证据：
  - catalog `ItemDetailVO` 使用 `itemId`
  - catalog `SpecVO` 使用 `specId`
  - common `ServiceItemDTO` 使用 `id`
  - common `ServiceSpecDTO` 使用 `id`
  - order `OrderServiceImpl.findAvailableSpec()` 使用 `spec.getId()` 匹配规格
- 修复任务：
  - [x] 统一 catalog 详情响应字段为文档契约：`id`、`specs[].id`。
  - [x] 或在 VO/DTO 上增加 `@JsonProperty` 兼容 `itemId/specId` 与 `id`。
  - [x] 删除或合并 `CatalogServiceFeignClient` 中重复路径的 `getItemDetail` / `getItemPrice` 设计。
  - [x] 增加 catalog-to-order Feign 契约测试。
  - [x] 增加下单创建订单集成测试：指定 `serviceItemId=201`、`serviceSpecId=301` 可成功生成订单。
- 验收标准：
  - [x] order-service 能正确反序列化 catalog 详情。
  - [x] 下单时能匹配规格、写入服务快照、价格快照。
  - [x] 规格下架或不存在时返回文档约定错误码。

### P0-03 feedback 与 order-service 的 Feign 契约不闭环

- [x] 状态：已修复
- 影响模块：`nursing-feedback-service`、`nursing-order-service`、`nursing-common`
- 对应需求：评价/投诉提交前通过 order-service 校验订单归属和状态
- 问题证据：
  - `OrderFeignClient.getOrder()` 不传 `X-User-Id`
  - `OrderController.detail()` 要求 `X-User-Id`
  - `OrderDetailResponse` 不包含 `userId`、`serviceItemId`
  - `ReviewServiceImpl.requireReviewableOrder()` 依赖 `OrderDTO.userId`、`serviceItemId`
- 修复任务：
  - [x] 设计内部订单查询接口，例如 `GET /internal/v1/orders/{id}`。
  - [x] 内部接口返回 `OrderDTO` 需要的字段：`orderId`、`orderNo`、`userId`、`status`、`serviceItemId`、`serviceItemName`、`specName`、`totalAmount`。
  - [x] 为内部接口增加服务间鉴权方案，避免公开暴露。
  - [x] 或让 feedback Feign 透传当前用户 ID，并确保 order 返回可校验字段。
  - [x] 修改 feedback 的评价/投诉前置校验逻辑。
  - [x] 增加评价、投诉提交集成测试。
- 验收标准：
  - [x] 已完成订单可提交评价。
  - [x] 非本人订单不可评价/投诉。
  - [x] 非已完成订单不可评价。
  - [x] 待服务或已完成订单可投诉。

### P0-04 支付回调未真实 RSA2 验签且未校验金额

- [x] 状态：已修复
- 影响模块：`nursing-order-service`
- 对应需求：API 文档要求支付宝回调免 JWT 但必须 RSA2 验签
- 问题证据：
  - `PaymentService.verifyCallback()` 在 `mockPayment=true` 时只校验参数存在和 `sign_type=RSA2`
  - `total_amount` 只入库，不与订单金额比对
  - `application.yml` 中 `nursing.payment.mock: true`，密钥为 `mock-public-key`
- 修复任务：
  - [x] 接入支付宝 SDK 或自行实现 RSA2 验签。
  - [x] 验签前按支付宝规则排除 `sign`、`sign_type` 并构造待签名字符串。
  - [x] 校验 `app_id`、`seller_id` 或商户号。
  - [x] 校验 `out_trade_no` 对应订单存在且状态为待支付。
  - [x] 校验 `total_amount == order.total_amount`。
  - [x] 处理非 `TRADE_SUCCESS` / `TRADE_FINISHED` 状态。
  - [x] mock 模式仅允许 dev profile，并在生产配置中强制关闭。
  - [x] 增加验签失败、金额不一致、重复回调、非待支付订单回调测试。
- 验收标准：
  - [x] 伪造签名无法更新订单状态。
  - [x] 金额不一致无法更新订单状态。
  - [x] 重复合法回调返回 `success` 且不重复入账。
  - [x] 合法回调写入 `payment_record`、更新订单状态、写入操作日志、写入 outbox。

## P1 重要缺陷

### P1-01 JWT secret 默认空导致登录/注册真实运行失败

- [x] 状态：已修复
- 影响模块：`nursing-user-service`、`nursing-gateway`
- 问题证据：
  - `nursing-user-service/src/main/resources/application.yml` 配置 `secret: ${NURSING_JWT_SECRET:}`
  - `TokenService.signingKey()` 在 secret 为空时抛 `SERVER_ERROR`
- 修复任务：
  - [x] 提供 `.env.example` 或 Nacos 配置样例。
  - [x] dev profile 提供长度足够的开发 secret。
  - [x] prod profile 禁止默认空值，启动时 fail-fast。
  - [x] Gateway 与 user-service 共享同一 secret 配置来源。
  - [x] 增加登录/注册签发 Token 的集成测试。
- 验收标准：
  - [x] 本地按说明启动后注册/登录可成功返回 JWT。
  - [x] Gateway 可校验 user-service 签发的 JWT。

### P1-02 订单状态流转缺少完成、退款和超时取消

- [x] 状态：已修复
- 影响模块：`nursing-order-service`、`nursing-feedback-service`
- 对应需求：系统架构设计订单状态图；评价前置要求订单已完成
- 问题证据：
  - 现有 `OrderController` 只有创建、查询、取消、支付入口
  - `rg` 未发现将订单状态置为 `2=已完成` 的业务方法
  - 取消待服务订单仅返回 `REFUNDING`，实际状态直接置为取消
- 修复任务：
  - [x] 增加服务完成接口或内部任务，将待服务订单变为已完成。
  - [x] 增加支付超时自动取消任务，默认 30 分钟。
  - [x] 增加待服务取消后的退款状态流转。
  - [x] 更新 `payment_record` 退款状态和退款时间。
  - [x] 所有状态变化写入 `order_operation_log`。
  - [x] 订单完成后发送 `ORDER_COMPLETED` 或明确 feedback 的可评价触发机制。
- 验收标准：
  - [x] 订单可从待支付到待服务到已完成。
  - [x] 超时未支付订单可自动取消。
  - [x] 待服务取消能进入退款流程而不是直接丢失退款状态。
  - [x] 完成订单可被评价。

### P1-03 Docker Compose 未编排业务服务与 Gateway

- [x] 状态：已修复
- 影响模块：部署目录、所有服务
- 对应需求：系统架构设计 Docker Compose MVP 单机部署
- 问题证据：
  - `docker-compose/docker-compose.yml` 只包含 MySQL、Redis、Nacos、Zookeeper、Kafka
  - 未包含 gateway、user、catalog、order、feedback
  - 服务 `application.yml` 使用 `localhost` 和 root 密码
- 修复任务：
  - [x] 为五个 Java 服务增加 Dockerfile 或 Maven jib/buildpacks 配置。
  - [x] 在 Compose 中增加 gateway 和四个业务服务。
  - [x] 服务间地址改为 Compose 网络内服务名。
  - [x] 通过环境变量注入 DB、Redis、Nacos、Kafka、JWT secret、支付配置。
  - [x] 增加健康检查和依赖启动顺序。
  - [x] 编写本地部署说明。
- 验收标准：
  - [ ] `docker compose up -d` 可启动完整后端。（外部阻塞：本机 Docker daemon 未运行）
  - [x] Gateway 路由可访问四个服务。
  - [x] MySQL 初始化脚本自动创建 4 个库和核心表。

### P1-04 身份证号未加密存储，SQL 日志存在敏感信息泄露风险

- [x] 状态：已修复
- 影响模块：`nursing-user-service`
- 对应需求：系统架构设计 AES-256 加密、API 脱敏、日志脱敏
- 问题证据：
  - `UserService.updateProfile()` 直接保存 `idCard`
  - `UserMapper.xml` 直接写入 `id_card`
  - `application.yml` 开启 `StdOutImpl` SQL 日志
- 修复任务：
  - [x] 增加身份证号加密组件，使用 AES-256 或等效方案。
  - [x] 配置加密密钥来源，避免硬编码。
  - [x] 写库前加密，读出后仅在必要场景解密并脱敏返回。
  - [x] 禁止生产环境 SQL stdout 参数日志。
  - [x] 增加日志脱敏过滤器。
  - [x] 增加 idCard 加密/脱敏单元测试。
- 验收标准：
  - [x] 数据库中 `id_card` 不存明文。
  - [x] API 返回身份证号始终脱敏。
  - [x] 日志中不出现完整身份证号。

## P2 一般缺陷与一致性问题

### P2-01 统一错误 HTTP 状态与文档不一致

- [x] 状态：已修复
- 影响模块：`nursing-common`、各业务服务
- 问题证据：
  - `GlobalExceptionHandler.handleBusinessException()` 固定返回 HTTP 400
  - 文档要求 401、403、404、409、422、429 等状态
- 修复任务：
  - [x] 让 `BusinessException` 支持 HTTP status。
  - [x] 或按错误码范围/具体 code 映射 HTTP status。
  - [x] user-service 的 `UserBusinessException` 与 common 异常模型对齐。
  - [x] 参数校验错误返回 `errors` 字段或明确统一格式。
- 验收标准：
  - [x] 未登录返回 HTTP 401。
  - [x] 无权限返回 HTTP 403。
  - [x] 资源不存在返回 HTTP 404。
  - [x] 重复提交返回 HTTP 409。
  - [x] 业务前置不满足返回 HTTP 422。

### P2-02 测试覆盖不足，缺少关键闭环测试

- [x] 状态：已修复
- 影响模块：所有服务
- 当前情况：`mvn test` 通过，但主要是 Mockito 单点测试
- 修复任务：
  - [x] 增加 Gateway 鉴权集成测试。
  - [x] 增加 user-service 注册/登录/登出/黑名单测试。
  - [x] 增加 catalog-order Feign 契约测试。
  - [x] 增加 order 创建订单幂等测试。
  - [x] 增加支付回调验签、金额、重复通知测试。
  - [x] 增加 feedback-order Feign 契约测试。
  - [x] 增加 Kafka Outbox 投递和消费幂等测试。
  - [x] 增加 Docker/Testcontainers 数据库初始化测试。
- 验收标准：
  - [x] `mvn test` 覆盖核心 happy path 和关键异常路径。
  - [x] 至少有一个跨服务契约测试能发现 DTO 字段不一致。
  - [x] CI 中执行全量测试。

### P2-03 Outbox / Kafka 消息格式未验证

- [x] 状态：已修复
- 影响模块：`nursing-order-service`、`nursing-feedback-service`
- 问题证据：
  - order 使用 `KafkaTemplate<String, String>`
  - order producer value serializer 配置为 `JsonSerializer`
  - feedback consumer value deserializer 配置为 `StringDeserializer`
- 修复任务：
  - [x] 明确 Kafka 消息值统一为 String JSON 或对象 JSON。
  - [x] 若使用 String，producer value serializer 改为 `StringSerializer`。
  - [x] 若使用对象，消费者改用 JSON deserializer 并配置类型映射。
  - [x] 增加 outbox 投递到 feedback 消费的集成测试。
- 验收标准：
  - [x] `ORDER_PAID` 事件可被 feedback 正确解析。
  - [x] 重复事件不会重复处理。
  - [x] 投递失败会重试并在 3 次后标记失败。

### P2-04 配置硬编码与环境隔离不足

- [x] 状态：已修复
- 影响模块：所有服务
- 问题证据：
  - 多个 `application.yml` 写死 `jdbc:mysql://localhost:3306/...`
  - 数据库用户名 `root`、密码 `root123`
  - Redis、Nacos、Kafka 均为 localhost
- 修复任务：
  - [x] 使用环境变量配置数据库、Redis、Nacos、Kafka。
  - [x] 提供 `application-dev.yml`、`application-prod.yml` 差异化配置。
  - [x] 移除生产环境 root 密码和本地地址。
  - [x] 提供 `.env.example`。
- 验收标准：
  - [x] dev 可本地运行。
  - [x] Docker Compose 可使用服务名运行。
  - [x] prod 不含硬编码 root 密码。

## P3 优化项

### P3-01 catalog 列表存在 N+1 查询

- [x] 状态：已完成
- 问题证据：
  - `ItemService.attachSpecs()` 对列表中每个 item 单独查规格
- 修复任务：
  - [x] 增加批量查询规格接口 `selectByItemIds`。
  - [x] 列表页按 itemId 分组一次性装配规格。
- 验收标准：
  - [x] 服务列表查询 SQL 数量不随列表长度线性增长。

### P3-02 catalog 种子数据使用 placeholder 图片

- [x] 状态：已完成
- 问题证据：
  - `02_catalog_schema.sql` 中分类 icon 使用 `https://via.placeholder.com/48`
- 修复任务：
  - [x] 替换为项目可访问的默认资源 URL。
  - [x] 或允许为空并由前端展示默认图。
- 验收标准：
  - [x] 服务浏览页面不依赖第三方 placeholder 资源。

### P3-03 文档与代码接口路径/方法需最终对齐

- [x] 状态：已完成
- 问题证据：
  - 架构文档曾出现 `/api/v1/orders/create`
  - API 文档和 order 计划最终采用 `POST /api/v1/orders`
  - 地址编辑在部分文档中出现 PUT/PATCH 差异
- 修复任务：
  - [x] 统一 API 文档、架构文档、计划书和代码路径。
  - [x] 生成 OpenAPI/Swagger 文档作为最终契约。
  - [x] 前端只按最终契约对接。
- 验收标准：
  - [x] 文档、网关路由、Controller 注解完全一致。

## 推荐修复顺序

1. [x] 修复 Gateway 鉴权与可信用户头。
2. [x] 配置 JWT secret，并跑通注册/登录。
3. [x] 修复 catalog-order DTO 契约，跑通下单。
4. [x] 修复支付回调验签与金额校验。
5. [x] 补齐订单完成/退款/超时状态流转。
6. [x] 修复 feedback-order 契约，跑通评价/投诉。
7. [x] 补 Docker Compose 完整部署。（`config` 已通过，完整启动受本机 Docker daemon 外部阻塞）
8. [x] 补跨服务集成测试和 Kafka Outbox 测试。
9. [x] 修复错误码 HTTP 状态、敏感数据加密、配置硬编码。
10. [x] 做 P3 优化和文档契约收口。

## 最小验收回归用例

- [x] 注册用户并返回 JWT。
- [x] 登录后 Gateway 能注入可信 `X-User-Id`。
- [x] 未登录访问订单接口返回 401。
- [x] 浏览分类、服务列表、服务详情。
- [x] 新增地址并设置默认地址。
- [x] 获取 prepay-token。
- [x] 使用同一个 Idempotent-Key 重复下单返回同一订单。
- [x] 发起支付。
- [x] 合法支付宝回调更新订单为待服务。
- [x] 非法签名回调被拒绝。
- [x] 订单完成后可提交评价。
- [x] 非本人订单不可评价/投诉。
- [x] 投诉提交后可查询投诉列表和处理记录。
- [x] Outbox 中 `ORDER_PAID` 事件可投递并被 feedback 幂等消费。
- [ ] `docker compose up -d` 后完整后端可启动并通过健康检查。（外部阻塞：本机 Docker daemon 未运行）

## 验证命令建议

```powershell
cd C:\Users\zhuzh\Desktop\project\nursing-platform
mvn clean test
docker compose -f docker-compose\docker-compose.yml config
```

修复 Docker 编排后增加：

```powershell
docker compose -f docker-compose\docker-compose.yml up -d --build
```


