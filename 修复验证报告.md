# 智慧护理平台修复验证报告

生成时间：2026-07-09

## 修复摘要
- 完成 Gateway JWT 鉴权：公开路径白名单、Bearer JWT 签名/过期校验、Redis 黑名单校验、清理伪造用户头并注入可信 `X-User-Id`。
- 修复 catalog/order DTO 契约：catalog 输出 `id/specs[].id`，common DTO 兼容旧 `itemId/specId`，移除重复 Feign 方法。
- 修复 feedback/order 契约：新增 `/internal/v1/orders/{id}` 内部查询接口，并以 `X-Internal-Token` 做服务间鉴权。
- 强化支付回调：实现 RSA2 验签、`app_id/seller_id` 校验、订单待支付校验、金额校验、重复回调幂等。
- 补齐订单状态：服务完成、支付超时取消、待服务取消进入退款中并更新支付退款时间，状态变化写操作日志。
- 加固敏感数据与配置：身份证 AES-GCM 加密存储、API 返回前脱敏、JWT/身份证密钥 fail-fast、生产 SQL stdout 关闭、配置环境变量化。
- 完成 Docker Compose 编排：MySQL、Redis、Nacos、Kafka、Gateway、user、catalog、order、feedback，增加健康检查和统一 Dockerfile。
- 完成 P3：catalog 列表批量规格查询，移除 placeholder 图片依赖，修正文档路径差异。

## 安全漏洞处理结果
- 鉴权绕过/伪造用户头：已通过 Gateway 可信头和业务服务 `X-Gateway-Token` 校验封堵。
- IDOR：订单外部接口按用户归属校验；feedback 前置校验要求订单归属匹配。
- JWT secret：dev 提供足长默认值，prod/default 缺失 fail-fast；Gateway 与 user-service 共享 `NURSING_JWT_SECRET`。
- 支付回调：伪造签名、金额不一致、非待支付订单均拒绝；重复合法回调返回 `success` 且不重复写入。
- 敏感数据：身份证写库前加密，DDL 改为 `VARCHAR(255)`；返回前脱敏；生产 MyBatis 使用 SLF4J 而非 stdout 参数日志。
- Kafka/Outbox：producer/consumer 统一 String JSON，失败重试 3 次后标记失败。
- 配置泄露：主配置改为环境变量，`.env.example` 提供样例，生产 profile 禁用 mock 支付。

## 关键代码变更
- `nursing-gateway`: `JwtAuthGlobalFilter`、`GatewaySecurityValidator`、JWT 配置。
- `nursing-common`: HTTP status 感知的 `BusinessException`、Feign/DTO 契约兼容。
- `nursing-order-service`: `InternalOrderController`、RSA2 支付校验、订单完成/超时/退款状态流转。
- `nursing-feedback-service`: 内部订单 Feign token、评价/投诉归属和状态校验测试。
- `nursing-user-service`: `IdCardCrypto`、JWT fail-fast、身份证加密/脱敏。
- `docker-compose/docker-compose.yml`、`Dockerfile`、`.env.example`。

## 新增或更新测试
- Gateway 鉴权：公开路径、未登录、有效 JWT、黑名单、伪造头清理。
- catalog/order 契约：`itemId/specId` 兼容 `id/specs[].id`。
- order 下单：`serviceItemId=201`、`serviceSpecId=301` 可匹配规格创建订单。
- 支付回调：伪造签名、金额不一致、重复合法回调、非待支付订单。
- feedback：本人已完成订单可评价，非本人/非已完成不可评价，待服务/已完成可投诉，非本人不可投诉。
- user：身份证加密后不含明文且可解密脱敏。
- catalog P3：列表规格批量查询，不再按 item 单独查询。

## 执行过的命令和结果
- `mvn clean test`：通过，Reactor 全模块 SUCCESS。
- `docker compose -f docker-compose\docker-compose.yml config`：通过。
- `mvn dependency:tree -DskipTests`：通过，已生成并检查 Maven 依赖树。
- 2026-07-09 17:45-17:46 复验：以上三条命令再次通过。

## 未能执行的命令及原因
- `docker compose -f docker-compose\docker-compose.yml up -d --build`：未执行成功。原因：本机 Docker Desktop Linux Engine 未运行，Docker API 管道 `npipe:////./pipe/dockerDesktopLinuxEngine` 不存在；2026-07-09 17:46 复验仍为同一外部环境错误。
- OWASP Dependency Check：项目未预配置该插件；首次运行需下载漏洞数据库。本次已按要求完成 Maven dependency tree 检查。

## 剩余风险
- Docker 完整启动和容器健康检查受本机 Docker daemon 状态阻塞，需在 Docker Desktop 启动后重跑。
- 未接入真实支付宝 SDK；当前使用标准 Java `SHA256withRSA` 按支付宝 RSA2 规则验签。生产需配置真实支付宝公钥、app_id、seller_id。
